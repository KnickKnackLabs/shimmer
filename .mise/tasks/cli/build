#!/usr/bin/env bash
#MISE description="Build the shimmer CLI (install Elixir dependencies)"
set -e

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
CLI_DIR="$(cd "$SCRIPT_DIR/../../.." && pwd)/cli"

cd "$CLI_DIR"
mix local.hex --force --if-missing 2>/dev/null
mix deps.get
