#!/usr/bin/env bash
#MISE description="Delete a blob from storage"
#USAGE arg "<key>" help="Object key to delete"

set -eo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
source "$SCRIPT_DIR/../_blob-env"

KEY="$usage_key"

mc rm "${BLOB_TARGET}/${KEY}"
echo "Deleted: ${KEY}"
