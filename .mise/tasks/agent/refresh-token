#!/usr/bin/env bash
#MISE description="Refresh the Claude OAuth token in GitHub secrets"
# DEPRECATED: This task is no longer needed.
# Pi uses ANTHROPIC_API_KEY (a static key) instead of Claude Code's OAuth token.
# Use `agent:sync-secrets --with-api-key` to sync the Anthropic API key.

set -e

echo "DEPRECATED: Claude Code OAuth tokens are no longer used."
echo "Pi uses ANTHROPIC_API_KEY instead. Sync it with:"
echo ""
echo "  shimmer agent:sync-secrets --with-api-key"
echo ""
exit 1
