#!/usr/bin/env bash
#MISE description="Stage all changes and commit with a message"
#USAGE arg "<message>" help="Commit message"

set -e

if [ -z "$1" ]; then
  echo "Usage: mise run commit <message>"
  exit 1
fi

git add -A
git commit -m "$1"
